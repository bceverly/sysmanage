# Development / lint / test tooling.  Pinned versions appear here when a
# version mismatch between local and CI has caused real friction; the rest
# float on purpose so security updates roll in without manual bumps.
#
# black is pinned because point releases reshape formatting and a local /
# CI version skew silently produces diffs that only surface in CI.
pytest
pytest-cov
pytest-asyncio
pylint
black==26.3.1
isort
bandit
safety
# Linux/macOS only — no OpenBSD/NetBSD/FreeBSD/Windows wheel (compiled OCaml core);
# without this marker pip backtracks through every sdist on the BSDs and never
# succeeds.  Mirrors the gate in requirements.txt.
semgrep ; sys_platform == "linux" or sys_platform == "darwin"
